#include <stdio.h>
int main()
{
    // char name = {"Abu Wahid"};
    // printf("%c", name);
    // char myText = {"Abu Wahid"};
    // printf("%c", myText);
    char name[] = {"Abu Wahid"};
    printf("%s", name);
    return 0;
}